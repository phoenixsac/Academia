#ifndef MODEL_H
#define MODEL_H

// This file includes other header files: student.h, faculty.h, course.h, and user_type.h. By including these headers, it allows the current file (model.h)
// to use the declarations and definitions from those headers without having to include each of them individually in every file that requires them.

#include "student.h"
#include "faculty.h"
#include "course.h"
#include "user_type.h"

#endif
